This version ensures SMS opt-in is optional:
- Submit button is always enabled.
- After submit: if checkbox checked, shows "subscribed" toast; if not checked, shows "not subscribed" toast.
- Use this when Twilio rejects with 30505 or 30498 over required consent.
